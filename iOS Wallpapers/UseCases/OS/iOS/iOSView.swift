//
//  iOSView.swift
//  Apple Wallpapers
//
//  Created by Adrián Castilla on 26/12/22.
//

import SwiftUI

/*
struct iOSView: View {
    var body: some View {
        
        List(iOSVersions) { ios in
            
            HStack {
                
                switch ios.name {
                case "iOS 16":Image("iOS 16 Logo")
                        .resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 15": Image("iOS 15 Logo")
                        .resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 14": Image("iOS 14 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 13": Image("iOS 13 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 12": Image("iOS 12 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 11": Image("iOS 11 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 10": Image("iOS 10 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                case "iOS 9": Image("iOS 9 Logo").resizable()
                        .frame(width: 50, height: 50, alignment: .trailing)
                        .clipped()
                default: Image("iOS Logo")
                }
                
                NavigationLink(ios.name, value: ios)
            }
            
        }
        .navigationDestination(for: iOS.self) { ios in
            switch ios.name {
            case "iOS 16": iOS16View()
            case "iOS 15": iOS15View()
            case "iOS 14": iOS14View()
            case "iOS 13": iOS13View()
            case "iOS 12": iOS12View()
            case "iOS 11": iOS11View()
            case "iOS 10": iOS10View()
            case "iOS 9": iOS9View()
            default: Text("uncoded")
            }
        }
        .navigationTitle("iOS")
    }
    
}

struct iOSView_Previews: PreviewProvider {
    static var previews: some View {
        iOSView()
    }
}
*/
